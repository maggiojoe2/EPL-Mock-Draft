// ── Shared draft constants ─────────────────────────────────────────────────

export const TOTAL_TEAMS = 12;
export const TOTAL_ROUNDS = 16;

/** Roster array length: index 0 is unused; indices 1–16 hold picks. */
export const ROSTER_SLOTS = TOTAL_ROUNDS + 1;
